import React from "react";

const Footer = ({ value, title }) => {
  return (
   
      <div>
        <h2>{title}</h2>
        <p> {value} </p>
      </div>

  );
};

export default Footer;
